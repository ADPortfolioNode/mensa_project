# =============================================================================
# Mensa project — ignore runtime/local artifacts; keep buildable source in git
# =============================================================================

# --- Python ---
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
.venv
ENV/
develop-eggs/
dist/
release/
downloads/
eggs/
.eggs/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST
.pytest_cache/
.mypy_cache/
.dmypy.json
dmypy.json
.pyre/
.tox/
htmlcov/
.coverage
.coverage.*
coverage.xml
*.cover

# Python build output (not application "build/" folders in frontend)
/backend/build/

# --- Node.js ---
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*
.pnpm-debug.log*
# Keep frontend lockfile for reproducible Docker/npm ci builds
package-lock.json
!frontend/package-lock.json
yarn.lock
pnpm-lock.yaml

# --- Frontend build output ---
frontend/build/
frontend/dist/
frontend/.cache/
frontend/build_exit_code.txt

# --- Runtime data, models, experiments (Docker volumes / local data) ---
data/chroma/
data/models/
data/cache/
data/experiments/
/data/
*.joblib
*.pkl
*.h5
*.parquet

# --- Logs & diagnostics (regenerated locally) ---
*.log
logs/
diag_output.log
diag_*.log
diag_fresh.log
startup_*.log
*_output.log
*_exit.log
.diag_start.lock

# --- Agent / IDE session artifacts (not application source) ---
terminals/
mcps/
agent-tools/
.snapshots/
message_from_agent.txt
.cursor/
!.cursor/rules/
!.cursor/rules/**

# --- Local verification outputs (scripts stay tracked; reports do not) ---
*_report.json
*_report_*.txt
production_test_report_*.txt
training_live_report.json
training_verify.json
verify_*_live.txt
verify_train_predict_strategy_report.json
all_games_run.json
train_*.json
!frontend/package.json
!frontend/public/manifest.json
!backend/requirements*.txt

# --- Scratch / temp ---
Removed/
Using/
*.bak
*.backup
*.old
*.orig
*~
*.swp
*.swo
.sublime-project
.sublime-workspace

# --- IDE ---
.vscode/
.idea/

# --- Environment & secrets (never commit) ---
.env
.env.local
.env.*.local
.env.production.local
.env.development.local
.env.test.local
.env.production
*.key
*.pem
*.p12
*.pfx
secrets/
credentials/
*.secret
*secret*
*api_key*
*apikey*
*api-key*
*token*

# --- OS ---
.DS_Store
Thumbs.db
.AppleDouble
.LSOverride
desktop.ini

# --- Docker local overrides ---
docker-compose.override.yml
Dockerfile.bak

# --- Deploy / hosting local state ---
.vercel
.env*.local

# --- Archives (keep source patches/docs; omit bundles) ---
*.zip
*.tar.gz
*.tgz

# --- Local databases ---
*.db
*.sqlite
*.sqlite3

# --- SSH keys ---
id_rsa
id_rsa.pub
id_ed25519
id_ed25519.pub