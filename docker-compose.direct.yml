# Publish frontend on HTTP when not using the Caddy TLS profile.
# Usage:
#   docker compose -f docker-compose.distribution.yml -f docker-compose.direct.yml up -d

services:
  frontend:
    ports:
      - "${FRONTEND_BIND:-127.0.0.1}:${FRONTEND_PORT:-3000}:80"